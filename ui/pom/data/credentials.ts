export const LOGIN_CREDENTIALS = {
	STANDARD_USER: process.env.STANDARD_USER || "default user",
	PASSWORD: process.env.PASSWORD || "default password",
	INVALID_USER: process.env.INVALID_USER || "invalid user",
	INVALID_PASSWORD: process.env.INVALID_PASSWORD || "invalid password",
};
