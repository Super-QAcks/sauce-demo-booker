import { test } from "./helpers/loginFixture.spec.ts";

test.describe("seed for logged in user", () => {
	test("seed using loginPage fixture", async ({ loginPage }) => {
		const page = loginPage; // eslint-disable-line
	});
});
